[project]
name = "connectrpc-hello-client"
version = "0.1.0"
description = "ConnectRPC Python client example"
requires-python = ">=3.8"
dependencies = [
    "connectrpc>=0.7.0",
    "protobuf>=4.24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
